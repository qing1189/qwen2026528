# === Qwen-2API 配置 ===

# Qwen 账号（邮箱:密码），多个用逗号分隔
# QWEN_ACCOUNTS=email1:password1,email2:password2

# 或直接提供 JWT 令牌，多个用逗号分隔
# QWEN_TOKENS=eyJ...,eyJ...

# API Key 保护（可选，设置后所有请求需携带此 Key）
# API_KEY=sk-your-key

# 服务端口（Docker 模式下容器内部固定 3000，此变量控制宿主机映射端口）
PORT=3000
